** start of script.js **

function bouncer(arr) {
  const array = []; //nueva
  for (let i = 0; i < arr.length; i++) {
    if (arr[i]) array.push(arr[i]); //agrego
  }
  return array;
}

bouncer([7, 'ate', '', false, 9]);

** end of script.js **

